/**
 * Shared types and enums used across multiple verification interfaces
 */

/**
 * Quality levels for various checks
 */
export enum QualityLevel {
  EXCELLENT = 'excellent',
  GOOD = 'good',
  FAIR = 'fair',
  POOR = 'poor',
  UNACCEPTABLE = 'unacceptable',
}

/**
 * Risk assessment levels
 */
export enum RiskLevel {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}
