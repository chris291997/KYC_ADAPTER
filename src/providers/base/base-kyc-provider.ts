// TODO: Update this base class to match the new IKycProvider interface
// For now, we'll export an empty placeholder to avoid compilation errors

export abstract class BaseKycProvider {
  // This will be properly implemented later when we have time to refactor
}
